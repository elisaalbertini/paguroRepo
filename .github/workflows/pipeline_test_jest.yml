on:
  push:
    branches:
      - main
      - develop
jobs:
  build:
    strategy:
        matrix:
          os: [ubuntu-latest]
    runs-on: ${{ matrix.os }}
    steps:
    - uses: actions/checkout@v4
    - run: cd ./paguro_web 
    - name: Install modules
      run: npm install
    - name: Run tests
      run: yarn test